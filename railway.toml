[build]
builder = "nixpacks"

[build.nixpacks]
pythonVersion = "3.11"

[deploy]
startCommand = "uvicorn mizan.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
